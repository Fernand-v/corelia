import { icsExportRequestSchema, smtpConfigSchema, webhookEndpointSchema } from "@corelia/types";

export const integrationSchemas = {
  smtpConfigSchema,
  webhookEndpointSchema,
  icsExportRequestSchema
};
